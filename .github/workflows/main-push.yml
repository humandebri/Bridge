# GitHub Actions: every main push runs the complete repository gate once.
name: bridge-main-push

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  full:
    uses: ./.github/workflows/ci.yml
    with:
      target_sha: ${{ github.sha }}
