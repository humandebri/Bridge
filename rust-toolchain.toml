# Repository root: pin the Rust compiler and Wasm target used by the Bridge canister build.
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
targets = ["wasm32-unknown-unknown"]
profile = "minimal"
