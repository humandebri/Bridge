{{! Build the Bridge staging artifact with the test-deployment feature in an isolated target dir. }}
build:
  steps:
    - type: script
      commands:
        - scripts/plan007/build-staging-canister-wasm.sh "$ICP_WASM_OUTPUT_PATH"
sync:
  steps: []
